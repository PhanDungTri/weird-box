import styled from "@emotion/styled";
import { COLOR } from "../constants";

const H1 = styled.div`
  font-size: 14px;
  padding: 8px;
  font-weight: bold;
  color: ${COLOR.Black};
`;

export default H1;
