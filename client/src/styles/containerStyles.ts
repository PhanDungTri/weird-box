import { css } from "@emotion/react";

export const gridStyle = css`
  display: grid;
  gap: 4px;
`;

export const pageStyle = css`
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
`;
